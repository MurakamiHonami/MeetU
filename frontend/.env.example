# ローカル dev: backend (wrangler dev) を直接指す。
# output: "export" では next.config の rewrites が効かないため、全環境で絶対 URL を指定する。
# Cookie は backend 側の CORS / SameSite 設定で共有する。
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8787
